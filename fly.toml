# fly.toml app configuration file generated for intern-fragrant-morning-7859 on 2025-07-16T16:39:03-07:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'intern-fragrant-morning-7859'
primary_region = 'sjc'

[build]

[processes]
  app = ""  # Default web process
  cron = "supercronic /app/crontab"  # Scheduled tasks

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
